# Dependencias opcionales (no son núcleo; el organismo corre con numpy+torch)
# Boca LLM (traductor congelado; sin él el organismo corre idéntico, ver doc 36):
mlx>=0.20
mlx-lm>=0.20
huggingface_hub>=0.25
# Benchmark MiniGrid piloto (N=5, retirado de la Tabla del paper):
gymnasium>=1.0
minigrid>=2.3
