numpy>=1.24
torch>=2.2
# Optional: LLM mouth (frozen translator, not required for core organism; behaviour identical without it per H2b)
# If not installed, boca() returns "[boca model not found - run download script]" and organism still runs
mlx>=0.20
mlx-lm>=0.20
huggingface_hub>=0.25
# Optional: MiniGrid benchmark pilot (N=5, underpowered; not required for main Table 1)
gymnasium>=1.0
minigrid>=2.3
